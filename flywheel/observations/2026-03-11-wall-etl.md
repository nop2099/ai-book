# Wall ETL Observations — 2026-03-11

## Source

- `~/w9/wall/.claude/skills/wall-etl/wall_etl.py`
- `~/w9/wall/boxes/scrape-summary.json`
- `~/w9/wall/boxes/known-boxes.json`

## Observations

- Box discovery is currently a reconciliation problem, not a single-source-of-truth lookup. `~/bin`, SSH config, known hosts, fingerprints, and Tailscale each know part of the fleet.
- Six explicit remote targets were discovered from the current evidence.
- Two explicit targets were reachable on the latest scrape: `ck` and `contabo-nop`.
- `ct` and `ctbd` hit host key mismatch errors for `173.249.56.193`.
- `contabo` failed root authentication on `217.77.0.36`.
- `dgx` timed out.
- `contabo-nop` exposed shell residue but not readable `~/.claude/projects`.
- `ck` exposed shell residue and a readable Claude project history.
- Remote shell residue is high-value operational context: histories, rc files, SSH config, and trust files explain how boxes are actually used.
- The ETL itself needed operational fixes: absolute binary paths for timer runs, overlap locking, and AppleDouble cleanup on the external drive.

## Candidate metrics suggested by these observations

- Reachable boxes / explicit boxes
- Host key drift incidents
- Auth failures by box
- Readable Claude histories by box
- Remote wall roots present by box
